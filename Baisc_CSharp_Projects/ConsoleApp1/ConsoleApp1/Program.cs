using System;
using System.Collections.Generic;




public class Number
{
    public int DigitalRoot(long n)
    {
        string num = n.ToString();
        Char[] numArray = num.ToCharArray();

        while (numArray.Length != 1)
        {
            for(int i = 0; i < numArray.Length || i > 1; i++)
            {
                numArray[i] =  
            }
        }
    }
}